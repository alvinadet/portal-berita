import React, { Component } from 'react';
import { CompCard, CompCardDetail, CompNavigasi } from '../components/index';

export default class Home extends Component {
  render() {
    return (
      <div>
        <CompCard />
      </div>
    );
  }
}
